spring:
  application:
    name: delivery-slot-service
  datasource:
    url: jdbc:postgresql://localhost:5432/delivery
    username: delivery
    password: delivery
  jooq:
    sql-dialect: postgres
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    com.picnic.delivery: INFO
